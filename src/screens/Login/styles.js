import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  subtitle_container: {
    paddingHorizontal: '10%',
    textAlign: 'center',
  },
  subtitle: {
    textAlign: 'center',
  },
  form: {
    paddingHorizontal: 20,
  }
})